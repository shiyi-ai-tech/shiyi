# 安全策略

## API Key 安全

史佚需要 LLM API Key 才能运行。请注意：

- **不要**将 API Key 提交到 Git 仓库
- `.env` 文件已被 `.gitignore` 排除，请确保不意外提交
- API Key 存储在 `~/.shiyi/.env` 中，仅本地可读
- 建议为史佚使用独立的 API Key，便于审计和吊销

## 数据安全

- 用户数据存储在 `~/.shiyi/` 目录下
- 记忆数据库为本地 SQLite，不联网传输
- WebUI 默认监听 `127.0.0.1:8530`，仅本机可访问
- 如需局域网访问，请注意网络安全

## 报告安全漏洞

如果你发现了安全漏洞，请**不要**通过公开 Issue 报告。

请发送邮件至项目维护者，包含：

1. 漏洞描述
2. 影响范围
3. 复现步骤
4. 建议修复方案（如有）

我们会在 48 小时内回复确认，并在修复后公开致谢。

## 已知安全考量

| 项目 | 说明 |
|------|------|
| WebUI 无认证 | 当前版本无用户认证，仅适合本地或受信网络使用 |
| Bash 工具 | 吏员的 bash 工具有危险命令黑名单，但不应暴露在公网 |
| API Key 存储 | 本地文件存储，依赖操作系统文件权限保护 |
